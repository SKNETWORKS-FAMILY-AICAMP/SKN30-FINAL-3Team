# infra/.env로 복사한 뒤 실제 12자리 계정 ID로 바꾼다. 자격 증명과 Secret은 넣지 않는다.
TARGET_ACCOUNT_ID=replace-with-12-digit-account-id
# 개인 CLI profile override. 미지정 시 just는 skn30-session을 사용한다.
# AWS_PROFILE=skn30-session
