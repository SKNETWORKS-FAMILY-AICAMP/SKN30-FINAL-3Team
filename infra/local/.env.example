# 로컬 Docker DB 전용 개인 입력. .env로 복사하고 값을 채운다(권한 600, Git 제외).
# Backend DB_URL은 backend/.env에서 별도 설정한다. 이 파일은 앱 설정 파일이 아니다.
# DB 초기 관리자 계정. 로컬 테스트 전용이며 공유 dev 계정을 재사용하지 않는다.
POSTGRES_USER=
# 위 계정의 개인 로컬 비밀번호. 추적 파일·로그에 기록하지 않는다.
POSTGRES_PASSWORD=
# 최초 생성할 로컬 개발 DB 이름. 테스트 DB와 구분한다.
POSTGRES_DB=
