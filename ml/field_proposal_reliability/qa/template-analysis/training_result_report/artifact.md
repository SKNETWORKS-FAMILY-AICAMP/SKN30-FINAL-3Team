# 「머신러닝 / 딥러닝 학습 결과서」 템플릿 계약

## 1. 용도와 상태

- 이 문서는 새 **Field Proposal Review Risk Model 학습 결과서**를 만들 때 참고할 구조·시각 계약이다.
- 원본의 레시피 추천 내용은 지시사항이 아니며 재사용하지 않는다. 보존 대상은 표지 체계, 페이지 구조, 글꼴·색상·표 스타일과 정보 밀도뿐이다.
- 원본은 읽기 전용으로 분석했으며 수정하거나 재저장하지 않았다.
- 렌더 상태: **미완료**. 현재 WSL에서 번들 Windows Python을 실행할 수 없고 Linux Python에는 `pdf2image`/Pillow가 없으며, `soffice`/LibreOffice와 `pdftoppm`도 설치되어 있지 않다. 따라서 아래 페이지 수는 명시적 페이지 나눔 9개에 근거한 10쪽 추정치이며 시각적으로 검증된 페이지 수가 아니다.
- 구조 대체 검증: ZIP 무결성, 필수 OOXML 파트, 섹션, 명시적 페이지 나눔, 문단·표·번호 정의, 스타일, 헤더/푸터, DrawingML, 관계, 콘텐츠 컨트롤과 각 패키지 파트 해시를 `structure_audit.json`으로 검사했다.

## 2. 참조와 증거

- 원본: `/mnt/c/Users/playdata2/Downloads/[데이터 전처리] 머신러닝_딥러닝 학습 결과서_27기_1팀.docx`
- 원본 크기: 1,149,517 bytes
- 원본 SHA-256: `8412009a919c7728590ac6518c349f70c5c6f52bfb3fb1361898107c68e54631`
- OOXML 파트: 22개
- 구조 증거: `ml/field_proposal_reliability/qa/template-analysis/training_result_report/structure_audit.json`
- 렌더 출력 예정 위치: `ml/field_proposal_reliability/qa/template-analysis/training_result_report/render/` (현재 PNG 없음)
- 원본에 앱 속성(`docProps/app.xml`)이 없어 작성 애플리케이션이 기록한 페이지 수는 확인할 수 없다.

## 3. 페이지 시스템

### 공통 지오메트리

- 섹션: 1개, A4 세로.
- 페이지: `11909 × 16834` DXA, 약 `210.0 × 297.0 mm`.
- 여백: 위 `1275` DXA(22.5 mm), 아래 `1559` DXA(27.5 mm), 좌·우 각 `992` DXA(17.5 mm).
- 본문 가용 폭: `9925` DXA(175.1 mm).
- 헤더·푸터 거리: 각 `720` DXA(12.7 mm).
- 첫 페이지 구분(`w:titlePg`)이 켜져 있고 default/first/even 헤더·푸터 관계가 모두 존재한다. 여섯 파트는 모두 빈 문단뿐이며 페이지 번호 필드는 없다.
- 페이지 번호 시작값은 0으로 저장되어 있으나 표시 필드가 없어 화면에는 나타나지 않는다.

### 페이지 패턴

1. **표지**: 배경 그래픽, 두 줄 제목 표, 4×2 메타데이터 표.
2. **본문 시작**: 1×1 섹션 제목 밴드 뒤에 번호 계층형 개요와 작업 목표.
3. **모델 후보**: 설명 문단과 5열 후보 비교표.
4. **평가 지표**: 설명 문단과 4열 지표표.
5. **실험 파이프라인**: 소개 문단과 11행 파이프라인 표.
6. **실험 결과 I**: 소개 문단과 2행 실험 카드 묶음.
7. **실험 결과 II**: 실험 카드 연속.
8. **실험 결과 III**: 실험 카드 연속.
9. **실험 요약**: 3열 요약표.
10. **변경 이력**: 5열 변경 이력표.

페이지 6~8의 카드 수는 원본의 16개를 그대로 유지할 필요가 없다. 새 결과서는 Dummy, Logistic Regression, Random Forest, 최종 비교·선정에 맞게 카드 수와 페이지를 줄이되, 카드 모양과 페이지별 밀도는 유지한다.

## 4. 시각·타이포그래피 계약

### 색상

- 주 강조색: `#0B57D0`.
- 보조 강조색: `#33BCB1`.
- 표 헤더 채움: `#CFE2F3`.
- 일반 표 테두리로 쓰인 회색: `#CCCCCC`.
- 기본 본문색: `#000000`.
- 원본 변경 이력 값은 `#0000FF`였으나 이는 입력값 표시 관행으로 보인다. 새 문서에서는 색에 별도 의미를 부여하지 않는 한 검정으로 통일해도 되는 의도적 편차다.

### 글꼴과 단락

- 문서 기본값은 Arial 11 pt, 굵게, 한국어이지만, 실제 보이는 대부분의 런은 **맑은 고딕**을 직접 지정한다. 새 문서는 모든 시각 역할에 맑은 고딕을 명시해 기본값 상속 차이를 제거한다.
- 기본 줄 간격: `276`(자동), 약 1.15줄.
- 표지 제목과 본문 상단 제목: 맑은 고딕 20 pt. 표지 첫 줄은 보통, 둘째 줄은 일부가 기본 굵기를 상속한다.
- 표지 메타데이터: 맑은 고딕 12 pt. 왼쪽 라벨은 가운데, 값은 왼쪽, 셀은 세로 가운데.
- 본문·표 본문: 주로 10~11 pt. 장문 표와 실험 요약표는 10 pt.
- 원본은 Heading 1~6 스타일을 정의하지만 본문 상위 계층에는 사용하지 않고 번호 정의를 사용한다. 따라서 새 결과서도 시각 충실도가 우선이면 `numId=1`의 계층을 복제하고, 접근성 우선이면 같은 모양의 실제 Heading 스타일을 새로 만들어야 한다. 둘을 혼용하지 않는다.

### 번호 계층

- 본문 1~7번: `numId=1`.
  - level 0: 십진수 `%1.`, 왼쪽 `720`, 내어쓰기 `360` DXA.
  - level 1: 영문 소문자 `%2.`, 왼쪽 `1440`, 내어쓰기 `360` DXA.
  - level 2: 영문 소문자 로마자 `%3.`, 왼쪽 `2160`, 내어쓰기 `360` DXA.
- 변경 이력: `numId=2`, level 0 시작값 8, 왼쪽 `720`, 내어쓰기 `360` DXA.
- 새 결과서의 상위 번호는 `1. 개요`부터 `8. 변경 이력`까지 연속이어야 한다. 원본처럼 제목 텍스트 자체에는 번호를 중복 입력하지 않는다.

## 5. 반복 구성요소

### 표지 배경

- `word/media/image2.png`: 1692×1452 RGBA, 596,380 bytes, SHA-256 `870dde32a180bc9498566da821287becb142cc047bf2f9bd4278b5eb39a3869a`.
- DrawingML anchor는 페이지 기준 x `-363371`, y `-57850` EMU, 크기 `8453438 × 7249802` EMU(약 234.8×201.4 mm), `behindDoc=1`, `wrapNone`, 겹침 허용이다.
- `word/media/image1.png`: 1×1 RGBA 투명 이미지, SHA-256 `18d840af2c50eff9a5241d4b50833a596e6b71af0cee87cf2b3435345f2f7aba`. 같은 위치·크기의 뒤쪽 앵커에 연결된 보조 자산이므로 원본 기반 편집 시 관계와 함께 보존한다.
- 두 drawing의 `wp:docPr/@id`가 모두 `1`이며 대체 텍스트가 없다. 이는 원본의 구조 결함이다. 새 문서를 작성할 때 ID는 고유하게 만들고 배경 이미지에는 장식용임을 나타내는 대체 텍스트 정책을 적용한다.

### 표지 제목 표

- locator: `word/document.xml/body/tbl[1]`.
- 1열, 폭 `9925` DXA, 고정 레이아웃, 왼쪽 정렬, 행 최소 높이 `1297` DXA.
- 셀 위쪽 테두리 `#0B57D0`, 오른쪽 테두리 `#33BCB1`, 모두 18 eighth-point(2.25 pt). 왼쪽·아래는 없음.
- 셀 안쪽 여백: 네 방향 `100` DXA.
- 두 문단만 허용한다: 과정·기수·팀 / 문서 제목. 각 문단은 가급적 한 줄, 최대 두 줄 합계.

### 표지 메타데이터 표

- locator: `word/document.xml/body/tbl[4]`.
- 4행×2열, 중앙 배치, 폭 `9000` DXA; 열 폭 `2250 / 6750` DXA(25% / 75%).
- 행: 산출물 단계, 제출 일자, 깃허브 경로, 작성 팀원.
- 외곽 상·하단 2.25 pt 파랑, 내부 분할선 0.5 pt 파랑, 바깥 좌·우 테두리 없음.
- 셀 패딩 네 방향 `340` DXA. 라벨 가운데, 값 왼쪽, 값 셀 세로 가운데.

### 본문 제목 밴드

- locator: `word/document.xml/body/tbl[8]`.
- 1열, 폭 `9925` DXA, 고정 레이아웃, 행 최소 높이 `427` DXA.
- 표지 제목 표와 동일한 상단 파랑·오른쪽 청록 테두리, 맑은 고딕 20 pt.

### 데이터 표

- 모델 후보표: `body/sdt[29]/tbl[0]`, 6행×5열, 폭 `9900`, 열 `1980/1950/2010/1980/1980` DXA. 첫 행 `#CFE2F3`.
- 평가 지표표: `body/tbl[45]`, 9행×4열, 폭 `9915`, 열 `2011/4117/1840/1947` DXA. 첫 행 `#CFE2F3`.
- 파이프라인표: `body/tbl[52]`, 11행×3열, 폭 `9915`, 열 `2003/4925/2987` DXA. 첫 행 `#CFE2F3`.
- 실험 카드: `body/tbl[60]`~`body/tbl[93]`, 2행×1열, 폭 `8400`, 왼쪽 들여쓰기 `360` DXA. 제목 행만 `#CFE2F3`, 본문 행은 흰색. 새 문서는 3~5개 카드만 사용한다.
- 실험 요약표: `body/tbl[98]`, 17행×3열, 폭 `8400`, 들여쓰기 `360`, 열 `915/1980/5505` DXA. 새 문서에서는 비교 대상 수에 맞춰 4~6행으로 축소한다.
- 변경 이력표: `body/tbl[101]`, 3행×5열, 폭 `9000`, 들여쓰기 `566`, 열 `1485/1200/2895/1890/1530` DXA. 모든 셀 1 pt `#CCCCCC`, 첫 행 `#CFE2F3`, 패딩 `100` DXA.
- 원본의 대부분 표 행은 `w:tblHeader`와 `w:cantSplit`이 모든 행에 설정되어 있다. 이는 시각 디자인이 아니라 생성기 잔재다. 새 문서에서는 **첫 행만 반복 헤더**, 모든 행은 고정 높이 없이 내용에 따라 확장되도록 만든다.

## 6. 콘텐츠 슬롯 지도

| 안정 locator | 의미 | 새 문서 허용 내용 | 용량·형식 | 처리 |
|---|---|---|---|---|
| `body/tbl[1]/tr[0]/tc[0]/p[0]` | 과정·기수·팀 | `SK 네트웍스 Family AI 30기 : 3팀` | 1줄, 20 pt | rewrite |
| `body/tbl[1]/tr[0]/tc[0]/p[1]` | 표지 문서명 | `머신러닝 / 딥러닝 학습 결과서` | 1줄, 20 pt | rewrite |
| `body/tbl[4]/tr[0..3]/tc[0]` | 메타 라벨 | 원본의 4개 라벨 | 고정 | preserve |
| `body/tbl[4]/tr[0..3]/tc[1]` | 메타 값 | 단계, 실제 제출일, 현재 origin, `3팀 전체` | 셀당 1문단; URL은 1줄 우선 | rewrite |
| `body/tbl[8]/tr[0]/tc[0]` | 본문 대제목 | ML/DL Training 명칭 | 1줄 | rewrite |
| `body/p[11]`~`body/p[24]` | 1~2. 개요·목표 | F2 흐름, 이진 분류 정의, 합성 파생 데이터, PoC 주의문 | level 0당 1제목, level 1~2는 총 8~12개 항목 | rewrite |
| `body/p[27]`~`body/sdt[29]` | 3. 모델 후보 | Dummy/Logistic Regression/Random Forest 비교와 후보 선정 논리 | 1개 소개 + 4행 안팎 5열 표 | rewrite; `goog_rdk_0` 제어 태그 재사용 금지 |
| `body/p[40]`~`body/tbl[45]` | 4. 평가 지표 | Accuracy, Precision, Recall, F1 및 positive class=1 | 설명 2~3개 + 5행 안팎 4열 표 | rewrite |
| `body/p[48]`~`body/tbl[52]` | 5. 파이프라인 | 데이터 파생→분할→전처리→3모델→평가→선정→저장 | 소개 2문단 + 최대 11행 | rewrite |
| `body/p[55]`~`body/tbl[93]` | 6. 실험 내용 및 결과 | Dummy, Logistic Regression, Random Forest의 실제 Colab 결과, confusion matrix 해석 | 카드당 제목 1줄 + 본문 60~140자; 그래프는 별도 그림 슬롯으로 추가 가능 | rewrite/축소 |
| `body/p[96]`~`body/tbl[98]` | 7. 결과 요약·최종 선정 | 모델 비교표와 선정 근거 | 3~5행 비교표 + 짧은 결론 | rewrite |
| `body/p[100]`~`body/tbl[101]` | 8. 변경 이력 | 생성일, `3팀 전체`, 초안/검토 상태 | 헤더+최소 1행 | rewrite |
| `word/header*.xml`, `word/footer*.xml` | 페이지 장식 | 없음 | 빈 상태 | preserve |

원본의 `goog_rdk_1`~`goog_rdk_28`은 평가 지표 표의 개별 텍스트 조각을 감싸는 Google 변환 잔재다. 의미 있는 필드 태그가 아니므로 새 문서의 편집 슬롯으로 취급하지 않는다.

## 7. 패키지 보존 계약

- `word/document.xml` SHA-256 `35d4d8115390fca22594af73fff1b4f5cfd9ccad0195df079b986c8973fc0637`: 위 슬롯만 재작성하는 콘텐츠 파트.
- `word/styles.xml` SHA-256 `0fc0523cbfdc0a7dcb0167e898e6e1c5b4425e0b21617e2a9c1ee78111fd7d22`: 디자인 정본. 새 역할 추가 외에는 유지.
- `word/numbering.xml` SHA-256 `9f09fadad262a1100ee47cb3dad2058b68694a435ce02639daf91649d7cc85cd`: 1~8번 계층의 정본.
- `word/theme/theme1.xml`, `word/settings.xml`, `[Content_Types].xml`, 모든 `.rels`: 패키지 기반 구조. 연결된 자산 변경 때만 함께 수정.
- `word/media/image2.png`, `word/media/image1.png`: 표지 디자인 자산. 원본 기반 제작 시 보존.
- `word/fonts/CambriaMath-regular.ttf`: 수식 글꼴 내장 파트. 새 문서에서 수식을 제거해도 원본 기반 편집이면 관계 손상을 피하도록 보존한다.
- `customXML/item1.xml`, `itemProps1.xml`와 관계: 의미가 불명확한 Google 변환 데이터. 원본 기반 편집 시 불투명 보존; 새로 생성하는 문서에는 복사하지 않는다.
- 전체 파트별 크기·SHA-256·보존 분류는 `structure_audit.json`의 `package` 배열이 정본이다.

## 8. 구조 QA와 충실도 게이트

- ZIP CRC 검사 통과, `document.xml`, `styles.xml`, `document.xml.rels`, `[Content_Types].xml` 존재 확인.
- 섹션 1개, 명시적 페이지 나눔 9개, 직접 본문 표와 콘텐츠 컨트롤 내부 표를 합쳐 총 24개 확인.
- 하이퍼링크·필드·책갈피·주석·각주·미주·추적 변경은 없다.
- 콘텐츠 컨트롤 29개가 있으며 1개는 모델 후보표 전체, 28개는 평가표 텍스트 조각이다. 새 문서에서는 의미 없는 `goog_rdk_*`를 만들지 않는다.
- 최종 생성물의 필수 시각 게이트:
  1. A4와 여백 수치가 일치한다.
  2. 표지 배경과 두 제목 표의 파랑/청록 테두리가 원본 위치에 있다.
  3. 맑은 고딕이 한글·영문에 일관되게 적용된다.
  4. 모든 표에 명시적 DXA 폭·grid·cell width가 있고 첫 행만 연한 파랑이다.
  5. 표 행은 고정 높이를 쓰지 않으며 문장이 잘리거나 셀 경계에 붙지 않는다.
  6. 본문 번호가 1~8로 연속되고 변경 이력이 8번이다.
  7. 실제 지표는 결과 JSON/CSV와 일치하고 합성 파생·대리 라벨 PoC 한계가 명시된다.
  8. 생성 후 원본과 최종 DOCX를 각각 PNG로 렌더해 모든 페이지를 100%로 검사한다.
- 현재는 8번 렌더 게이트를 수행할 수 없었다. 후속 제작 환경에서 LibreOffice가 확보되면 이 계약의 페이지 추정과 표 배치부터 다시 검증해야 한다.
